interface:
  display_name: "PRD Lifecycle Manager"
  short_description: "Readable PRDs with stories, use cases, and tracking"
  default_prompt: "Use $prd-lifecycle to create or update a readable, versioned PRD with user stories, use cases, acceptance criteria, and traceable delivery status."
